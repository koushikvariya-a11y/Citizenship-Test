{
  "entities": {
    "profile": {
      "title": "UserProfile",
      "description": "User profile containing federal state selection and practice index",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user matching request.auth.uid"
        },
        "selectedState": {
          "type": "string",
          "description": "The chosen federal state"
        },
        "practiceIndex": {
          "type": "number",
          "description": "The last practice index practiced by the user"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the profile was last modified"
        }
      },
      "required": ["userId", "selectedState", "practiceIndex", "updatedAt"]
    },
    "attempt": {
      "title": "QuizAttempt",
      "description": "Saved answer choice attempt for a specific question",
      "type": "object",
      "properties": {
        "questionId": {
          "type": "number",
          "description": "ID of the question attempted"
        },
        "selectedIdx": {
          "type": "number",
          "description": "The answer option index chosen"
        },
        "isCorrect": {
          "type": "boolean",
          "description": "Whether the answer was correct"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The ISO timestamp of when the question was completed"
        }
      },
      "required": ["questionId", "selectedIdx", "isCorrect", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "profile",
      "description": "Stores high-level user state"
    },
    "/users/{userId}/attempts/{attemptId}": {
      "schema": "attempt",
      "description": "Stores granular history of user quiz attempts"
    }
  }
}
